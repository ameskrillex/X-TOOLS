X-Tools 3.5.207 — исправление ответов о семье

- Исправлен разбор запросов вида `144 id family?`: раньше они превращались в ник `144_id`, сервер не находил такого игрока, и очередь ждала тайм-аут 8 секунд.
- Метки `id`, `ид` и `айди` перед или после номера теперь определяют ID игрока. Поддерживаются заглавные буквы, лишние пробелы и варианты `id: 144` / `id #144`.
- Обычные запросы `196 family`, запросы по нику, пользовательские ключевые фразы и ручная команда `/fama` сохранены. Настоящие ники с подчёркиванием, например `144_id`, не изменяются.
- После получения статистики отправляется ответ о семье; следующие запросы больше не задерживаются из-за неверного ника, созданного из метки ID.

Обновление: `/update`, затем рекомендуется полностью перезапустить игру. Настройки и журналы сохраняются. Совместимый канал для старых версий по-прежнему использует переходный загрузчик 3.5.205.

Проверка: 10 тестов LuaJIT с воспроизведением ошибки в 3.5.206, вариантами запросов и полной цепочкой «сообщение → /offst → диалог → /ans → следующий запрос». Проверены смена получателя, отключённый игрок, чужой диалог, компиляция Lua, контрольные суммы обоих каналов и содержимое полного архива. Проверка непосредственно в GTA SA ещё не выполнялась; выпуск предварительный.
